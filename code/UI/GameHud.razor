@using Sandbox;
@using Sandbox.UI;
@namespace Battlebugs
@inherits PanelComponent

<root>
	<div class="enable-mouse" />

	@if (GameManager.Instance.State == GameState.Waiting)
	{
		<WaitingHud />
	}
	else if (GameManager.Instance.State == GameState.Placing)
	{
		<PlacingHud />
	}
</root>

@code
{
	protected override int BuildHash() => System.HashCode.Combine(GameManager.Instance.State);
}